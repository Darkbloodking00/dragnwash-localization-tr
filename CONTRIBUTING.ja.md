# Contributing（翻訳への参加）

[English](CONTRIBUTING.md)

Drag'n Wash Localizationは、コードを書かなくても **CSV を編集するだけ**で翻訳に参加できるModです。
このページは翻訳に参加したい方向けです。プラグイン本体をビルドしたり配布したりする開発者向けの手順は [docs/RELEASING.ja.md](docs/RELEASING.ja.md) にあります。

参加するときは[行動規範](docs/CODE_OF_CONDUCT.ja.md)を守ってください。
セキュリティの問題は、プルリクエストや公開のIssueには書かずに、[SECURITY.ja.md](SECURITY.ja.md) にある非公開のフォームから知らせてください。

## 必要なもの

- Drag'n Wash（Steam版）と本Modの導入（BepInEx）
- CSVを編集できるテキストエディター（Excel / LibreOffice / VS Codeなど）

Unityの内部キー名もプログラミングの知識もまったく要りません。作業中は**画面に表示される英語原文そのもの**を `source_en` に書いておき、
コミットの前にそれをハッシュに変えます（やり方は下の「コミット前にハッシュ化する」にあります）。

### 言語の表示名（`name.txt`）

`Translations/<locale>/name.txt` に書いた文字列が、言語の名前として次の場所にそのまま表示されます。

- ゲームの **Options →「言語（Mod）」** の一覧
- F1メニューの言語ボタン
- インストーラー（Windows・Steam Deck）の言語選択

たとえば `ja/name.txt` なら `日本語`、`zh-Hans/name.txt` なら `简体中文` です。
中身は1行だけにして、UTF-8で保存してください。ファイルがないときはフォルダー名が出ます。
新しい言語を足すときに作るのは、フォルダーと `strings.csv`、`name.txt` の3つだけです。

### 状態と見た人（`credits.txt`）

F1メニューの **About** タブの言語の表は、`Translations/<locale>/credits.txt` を読んで出しています。
1行目がパックの状態で、`supervised`、`proofread`、`converted`、`provisional`、`fun` のどれかです。
2行目からは、そのパックを見てくれた人の名前を1行に1人ずつ書きます。
ファイルがないパックは provisional として出ます。
PRでこのファイルを触る必要はありません。ほかのクレジットと一緒にメンテナーが直します。

## 基本の流れ

1. このリポジトリをフォークします。
2. `Translations/<locale>/strings.csv` に訳を追加したり直したりします。
3. コミットしてPull Requestを送ります。タイトルと本文の書き方は「[Pull Request の書き方](#pull-request-の書き方)」を見てください。

## 翻訳ファイルの形式

> [!IMPORTANT]
> **v0.6.0 から CSV の書き方が一部変わりました。**
> 前のファイルもそのまま読み込めますが、今のファイルを編集するときは次の違いに気を付けてください。
>
> - **台詞 ID の行:**
>   `key` 列には、16 桁のハッシュだけでなく `line:6046bedf` のような Yarn の台詞 ID も入ります。
>   そういう行は、その 1 つの台詞にだけ使う訳です（「[複数のキャラが話す台詞](#複数のキャラが話す台詞)」を見てください）。
> - **複数の話者:**
>   複数のキャラが話す台詞だと、`speaker` 列に全員が `/` 区切りで並びます（`Ryan/Alexander`）。
> - **作業コピー:**
>   共有されている台詞が出てくる位置ごとに、訳が空の台詞 ID の行が足されています。
>   訳し分けたいのでなければ、空のままでかまいません。
> - **`order`:**
>   会話の中の行をすべて数えるようになったので、v0.6.0 より前のファイルとは番号が違います。
>   読むときの目安の列なので、直さなくて大丈夫です。
>
> 翻訳するときは、v0.6.0 以降のプラグインと、このリポジトリのツールを使ってください。
> v0.5.0 以前のツールは台詞 ID の行を知らないので、壊れたハッシュの行に書き換えてしまいます。
> 古いバージョンで遊んでいる人は、台詞 ID の行が無視されるだけなので影響はありません。

`Translations/<locale>/strings.csv` は公開用のCSVです。行は**ゲームで流れる順**に並んでいて、`#` の見出しで区切られています。
作業中なら、原文つきの行を**同じファイルに混ぜて**も読み込めます。

```csv
key,section,node,order,speaker,translation

# ===== Level 1: Ryan (Sunny) | sets level_1 | ends level_1_complete =====
# --- intro: Ryan_1_intro ---
5d0a…,L01 Ryan,Ryan_1_intro,1,Ryan,よお。ここが洗い屋か？
# --- phone: Ryan_1_PhoneTutorial | if $has_talked_to_ryan ---
…
# ===== UI and other text (not part of the dialogue script) =====
d0db8b5e364b6989,UI,,,UI,オプション
```

- `section`:
  `L01 Ryan` のようにレベル番号とドラゴン名が入るか、`Cutscene` / `Reaction` / `Unused` / `UI` のどれかです。
- `node` / `order`:
  Yarnの会話ノード名と、その中での順番です。分岐先のノードは親のすぐ後ろに置かれます。
- `#` 行:
  見出しです。読み込むときは無視されるので、好きに残しておけます。
  `| if $変数` は分岐の条件を示すヒントです。
- 並び順は `data/script_order.csv` で決まります。入っているのはノード名・行ID・ハッシュ・話者だけで、英語は入っていません。
  ゲーム内の **Export game flow** で作り直せて、ゲームが更新されたときはメンテナーが作り直します。

```csv
source_en,translation
Options,オプション
```

- `key`:
  原文のSHA-256の先頭16桁です。**リポジトリにはこの形式だけ**が入ります。
  ゲームの英語台本を再配布しないためで、こうしておけば製品版を持っていない人は
  台本を読めませんし、原文なしで訳を書くこともできません。
- `speaker`:
  誰の台詞かが入ります（Conrad / Ryan / Alexander / Kobold＝選択肢 / Phone / UI）。
  台本の構造から自動で付きます。複数のキャラが話す英文なら、`Ryan/Alexander` のように全員が並びます。
- `key` には `line:6046bedf` のようなYarnの台詞IDも書けて、
  その行はその1つの台詞だけの訳になります（「[複数のキャラが話す台詞](#複数のキャラが話す台詞)」を見てください）。
- `source_en`:
  ゲームに表示される英語原文をそのまま書きます。一字一句同じかどうかで照らし合わせます。
  **作業中はこちら**で書けば、保存したとたんにホットリロードで画面に反映されます。
- `translation`:
  訳文です。

プラグインは画面に出た英文をその場でハッシュして探すので、どちらの行も同じように動きます。
F6 / F7で書き出したファイルには `key` 列と `source_en` 列の両方が入っているので、どれがどれに当たるかはそこで分かります。

### 新しい言語を始める

1. `Translations/<locale>/` フォルダー（例: `ko`）を作って、`name.txt` に表示名（例: `한국어`）を書く。
2. **Options → Mods → Drag'n Wash ModFramework → Developer tools** で開発者ツールをオンにする。
   初期設定はオフで、遊ぶだけの人にはF1の窓も書き出しも`_discovered` フォルダーも出ません。
   ゲームを起動して、**Options →「言語（Mod）」** かF1 → Translationの言語一覧で新しい言語を選ぶ。まだ訳が0件なので、画面は英語のままです。
3. **F1 → Translation → Export working copy** を押す。
   `strings.csv` がなくても、ゲームにある全部の行の英語原文を並べた空の作業コピー `_discovered/<locale>.working.csv` ができる。
4. ここから先は、下の「原文を並べて作業する」と同じ。
   訳した行から順に画面に反映されていく。
5. Optionsの項目名 `Language (Mod)`（キーは `e3becbaee46cc0df`）も訳す。
   ゲームにもともとある設定ではなくModの設定だとわかるように、「(Mod)」かその言語での言い方は残しておく。
   この項目は、一度Optionsを開くと作業コピーとF7の書き出しに出てくる。

### 原文を並べて作業する（推奨）

ゲームの中で **F1 → Translation → Export working copy** を押すと、公開用の `strings.csv` が
`Translations/_discovered/<locale>.working.csv` に展開されます。

```csv
key,section,node,order,speaker,source_en,translation
5d0a…,L01 Ryan,Ryan_1_intro,1,Ryan,Hey. This the cleaning place?,よお。ここが洗い屋か？
```

- **並び順と話者:**
  会話は**ゲーム内の実行順**に並んでいて、`speaker` 列には**誰の台詞か**が入ります
  （Conrad / Ryan / Alexander、選択肢はKobold、本部からの電話はPhone、UIはUI）。
  口調をそろえるときの目印にしてください。
- **`source_en`:**
  ゲームが今読み込んでいる台本とUIから原文が入ります。
  会話文までそろえたいときは、セーブをロードしてから押してください。
- **ホットリロード:**
  このファイルを編集して保存すれば、ホットリロードでその場で画面に反映されます。
- **コミットされない:**
  `_discovered/` の下にあるので、リポジトリには入りません。

プラグインがゲームの中で動いていること自体が「製品版を持っている」証明になるので、ほかに認証はありません。

### 複数のキャラが話す台詞

ハッシュの行は、英文が同じ台詞**すべて**に使われます。
短い台詞だと、別々のキャラが同じ英文を話すことがあります。
たとえば `Wonderful!` は、レベル1ではライアン、レベル5ではアレクサンダーの台詞です。
そういう行は `speaker` 列に `Ryan/Alexander` のように話者が全員並んでいて、1つの訳が全員に使われます。

1つの訳では収まらないときは、その台詞だけ別の訳にできます。
作業コピーには、共有されている台詞が話される場所ごとに、Yarnの**台詞 ID** をkeyにした空の行が入っています。

```csv
key,section,node,order,speaker,source_en,translation
84f325bca745e504,L01 Ryan,Ryan_1_intro,9,Ryan/Alexander,Wonderful!,全員に使う訳
line:6046bedf,L01 Ryan,Ryan_1_intro,9,Ryan,Wonderful!,ライアン専用の訳
line:ab423ac7,L15 Alexander,Alexander_5_required,19,Alexander,Wonderful!,
```

- 訳し分けたい台詞の行だけ埋めてください。
  訳が入った台詞IDの行はその1か所だけに使われて、ほかの場所はハッシュの行の訳のままです。
- 空の台詞IDの行は残しておいてかまいません。
  *Hash for commit* が書き出すのは訳が入った行だけで、台本の中の対応する位置に置かれます。
- 台詞IDはゲームの台本から来ているので、
  ゲームのアップデートでIDが変わった台詞は、何の知らせもなくハッシュの行の訳に戻ります。
- 台詞IDの行が使えるのは会話と選択肢だけで、UIの文字には使えません。

### コミット前にハッシュ化する

PRを送る前に、公開用の `strings.csv` を作り直してください。
作業ファイル（`_discovered/<locale>.working.csv`）があればそこから、
なければ `strings.csv` の中の `source_en` の行から作られます。

やり方は3つあります。

- ゲームの中で **F1 → Translation → Hash for commit** を押すと、今の言語のファイルが書き換わります。
- `tools/hash-strings.ps1` を引数なしで動かすと、全言語をまとめて作り直します。
- Dockerがあれば、どのOSでも手元に何も入れずにできます。
  1言語なら `docker compose run --rm hash ja`、全部なら `docker compose run --rm hash` です。
  [docs/DOCKER.ja.md](docs/DOCKER.ja.md) を見てください。

`-Path` を付けたときは動きが違います。
**渡したファイルをその場で変換するだけ**で、作業ファイルは探しません。
なので、公開用の `strings.csv` に使ってください。
作業ファイルを渡すと公開形式で上書きされて、`source_en` 列も未翻訳の行もすべて消えてしまいます。

**英語原文が残った `strings.csv` は PR で受け付けません。**
PRごとに自動チェックが走っていて、形式が違うと理由を英語でコメントします。
直してプッシュすれば、そのコメントが書き換わります。
プッシュする前に同じチェックを回したいなら、Dockerで `docker compose run --rm checks` を使ってください。

値にカンマや引用符、改行が入るときは、フィールドを `"` で囲んでください。引用符そのものは `""` と書いてエスケープします。
細かいところは [RFC 4180](https://datatracker.ietf.org/doc/html/rfc4180) に沿っています。

### 書式タグについて

原文に `<size=70%>` / `<gradient="gold">` / `<i>` のようなTMPの書式タグが入っているときは、
**タグ構造はそのまま残して、中の文章だけ**訳してください。
タグが壊れると表示が崩れます。

```csv
"<gradient=""gold""><b> ...English... </b></gradient><size=70%> (hint)","<gradient=""gold""><b> ……訳文…… </b></gradient><size=70%>（ヒントの訳）"
```

## 未翻訳の原文を探す

ゲームを遊びながら、訳す原文を集める方法が3つあります。
どれも、ゲームの中でFキーを押すか自動記録で、`Translations/_discovered/` にCSVとして書き出されます。

| 方法     | 出力先                           | 内容                                                                                                                                          |
|----------|----------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------|
| **F6**   | `_discovered/dialogue_lines.csv` | 全会話文を**ゲーム内の実行順**で（`yarn_project,node,order,kind,speaker,line_id,key,source_en,translation,tags`）。セーブをロードした後に押す |
| **F7**   | `_discovered/ui_texts.csv`       | 全UIテキスト（非表示メニュー含む。`key,source_en,translation,object_path`）                                                                   |
| 自動記録 | `_discovered/strings.csv`        | プレイ中に見つかった未翻訳の原文（`source_en,translation`）                                                                                   |

`_discovered/` にはゲーム本体の著作物である会話文そのものが入っているので、
**リポジトリにはコミットしないでください**（`.gitignore` にも入っています）。
それぞれが手元で作るものです。

- `dialogue_lines.csv` から訳したい行を `strings.csv` にコピーして、
  `translation` 列だけ埋めればOKです。`node` や `line_id` のような余分な列が付いたままでも読み込めます。
- `node` 列は「キャラクター名＿何回目＿場面」（例: `Conrad_1_intro`）、
  `order` 列はその会話の中での順番、`kind` 列は `line`（台詞）か `option`（プレイヤーの選択肢）です。
  会話ごとにまとめて訳すと、口調や流れをそろえやすくなります。
  ドラゴンはConrad / Ryan / Alexanderの3体で、`RyanMuddy` のように後ろに何か付いているのは同じキャラクターの別の状態です。
- 訳した行は `translation` 列に訳が入ったまま書き出されるので、もう一度ダンプしても作業は消えません。
- `ui_texts.csv` の `object_path` 列を見れば、その文言が画面のどこにあるかが分かります。
  タイトル画面とゲーム中で1回ずつF7を押せば、UIはほぼ全部そろいます。

## 訳す必要のない文字列

スライダーの数値や解像度（`1920 x 1080 @ 164.995Hz`）、ビルド番号などは、はじめから記録しません。
ほかにも外したいものがあれば、`Translations/ignore.txt` に正規表現で書き足せます。書き方の例はファイルの中にあります。

除外が効くのは「記録」だけです。
翻訳を探すのはその前なので、`strings.csv`に書いた行は除外パターンに合っていても**必ず翻訳されます**。

## ロケール（言語）の追加

`Translations/<locale>/` にフォルダーを作って `strings.csv` を置くだけです。
ロケール名は、今あるものに合わせてBCP 47の形式にしてください（例: `ja`、`zh-Hans`、`zh-Hant`、`pt-BR`、`ko`）。
フォルダー名はプラグインが自動で見つけます。

フォントはファイルの中の文字を見て自動で選ぶので、たいていの文字体系では何もしなくて大丈夫です。

- **フォントがあるもの:**
  日本語、中国語（簡体字・繁体字）、韓国語、キリル文字、アクセント付きのラテン文字、ヘブライ文字なら、
  WindowsでもSteam Deckでもフォントが見つかります。
- **右から左に書く言語:**
  `he`・`ar`・`fa`・`ur`・`yi` は、自動で右から左に表示します。
  ファイルにはふだん入力する順のまま書いてください。行の中にラテン文字の単語や数字を混ぜると逆順に表示されるので、混ぜないでください。
- **ほかの文字体系:**
  合うフォントがない文字体系は、プラグインDLLの隣の `fonts/` フォルダーに `.ttf` / `.otf` を置いてください。

## 仮翻訳の言語を改善する

日本語と簡体字中国語以外の言語パックは、どれも仮翻訳です。全部の行はそろっていますが、ネイティブの人の確認はまだ受けていません。
なので、その言語を話せる方にレビューしてもらえるのがいちばん助かります。

- **行の修正:**
  直したい行があれば、PRで送ってください。
- **パック全体を確認したら:**
  ネイティブの方がパック全体を通して確認できたら、
  同じPRで `strings.csv` の先頭のコメントと、READMEの言語一覧にある状態も書き換えてください。
  それが難しければ、PRに「パック全体を確認した」と書いてもらえれば、メンテナーが両方を直します。
- **ハッシュ化は不要:**
  公開用の `strings.csv` の `translation` 列だけを直して、ほかの列をそのままにしておけば、
  ファイルはハッシュ化されたままなので *Hash for commit* は要りません。
- **原文を横に並べる:**
  英語の原文を横に並べて確認したいときは、「[原文を並べて作業する](#原文を並べて作業する推奨)」の作業コピーを使ってください。

## 動作確認

- ゲームの **Options →「言語（Mod）」** か、**F1** のデバッグウィンドウの **Translation** タブで言語を切り替えると、
  再起動しなくても表示が変わります。Optionsでは、Saveで確定し、Backを押すと保存してある言語に戻ります。
- Translationタブの **Check translation layout** を押すと、レイアウトが崩れそうな文字列が
  `_discovered/layout_risks.csv`（`source_en,translation,axis,required_px,available_px,ratio,object_path`）に書き出されます。
  `ratio` が大きいものほど大きくはみ出すので、訳文を短くするなどして調整してください。

## Pull Request を送る前に

- `source_en` が、実際に画面に出る英文と**完全一致**しているか。大文字と小文字、前後の空白、書式タグまで同じか。
- **ハッシュ化済みか**（`strings.csv` の列が `key,section,node,order,speaker,translation` で、`source_en` の行が残っていないか）。
- 書式タグの構造が原文と同じか。
- 重複した行や、`translation` が空の行を入れていないか。
- 1つのPRには、1つの言語の、ひとまとまりの範囲だけを入れてください。

## Pull Request の書き方

- **タイトル:**
  先頭に言語コードを角かっこで付けて、何を変えたかを書きます。
  たとえば `[ko] 韓国語訳の修正`、`[ko] レベル1〜3のネイティブチェック`、`[de] Options の項目名を翻訳` のようにします。
  英語、日本語、その言語のどれで書いてもかまいません。
- **本文:**
  PRを作ると、書き込むためのテンプレートが自動で入ります。
  **「What / 内容」**には言語と直した範囲（レベル、`Conrad_1_intro` のような場面、UIなど）を書いて、
  チェックリストは当てはまるものだけにチェックを入れてください。
  **「Credit / クレジット表記」**には、クレジットを載せてほしいかどうかと、
  載せる場合に出してほしい名前を書いてください。リンクも載せたいなら一緒に書いてください。
- **送ったあと:**
  まず翻訳の自動チェックが走ります。
  問題があれば、理由とファイル名・行番号が英語のコメントで届きます。直してpushすると、同じコメントが書き換わります。
  そのあとでメンテナーがレビューします。
  わからないことがあれば、PRに英語か日本語で気軽に書いてください。
  チェックで止められたときは「[自動チェックで止められたとき](#自動チェックで止められたとき)」を見てください。

## 自動チェックで止められたとき

PRが来るたびに `tools/check-translations.py` が動きます。
問題が見つかるとPRに英語のコメントが付いて、
理由の説明と、問題を `ファイル:行: メッセージ` の形で並べた **Full report** が出ます。
行番号は、ファイルをエディターで開いたときの行番号と同じです。
同じブランチに修正をpushするとチェックがやり直されて、コメントは増えずに同じものが書き換わります。

pushする前に、手元で同じチェックを走らせることもできます。Python 3.9以降が要ります。

```bash
python tools/check-translations.py
```

問題がなければ `translations OK` と表示されます。

| レポートのメッセージ                                                                 | 意味                                                                                   | 直し方                                                                                                               |
|--------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------|
| `header is [...]; the published file must be ...`                                    | ファイルが作業コピーのまま（`source_en` 列がある）                                     | **F1 → Translation → Hash for commit** か `tools/hash-strings.ps1` を実行し、作り直した `strings.csv` をコミットする |
| `key is not 16 lowercase hex digits or a line ID`                                    | key がハッシュでも台詞 ID でもない。key の列に英文が入っているか、key を書き換えている | *Hash for commit* で作り直す。`key` 列は手で編集しない                                                               |
| `must not be committed (contains source text)`                                       | `Translations/_discovered/` のファイルか `strings.local.csv` が PR に入っている        | `git rm --cached <ファイル>` で PR から外してコミットする（手元のファイルは残せます）                                |
| `duplicate key (see line N)`                                                         | 同じ行が 2 回ある                                                                      | key ごとに 1 行だけ残し、もう一方を消す                                                                              |
| `empty translation`                                                                  | `translation` が空の行がある                                                           | 訳を入れるか、行ごと消す（その行は英語で表示されます）                                                               |
| `expected 6 fields, got N`                                                           | 列の数が合わない行がある                                                               | `,`・改行・`"` を含む値をダブルクォートで囲み、中の `"` は `""` と書く                                               |
| `section does not look like an identifier` / `node does not look like an identifier` | この列を編集したか、列がずれている                                                     | `main` の値に戻し、`translation` 列だけを直す                                                                        |
| `no strings.csv`                                                                     | 言語フォルダーに `strings.csv` がない                                                  | ファイルを追加するか、空のフォルダーを消す                                                                           |
| `empty file`                                                                         | `strings.csv` に見出し行がない                                                         | 先頭に `key,section,node,order,speaker,translation` を書く                                                           |

書式タグが原文と同じ構造かどうかは、このチェックでは調べていないので、レビューで確認します。

**表計算ソフトは、保存するときにファイルを壊すことがあります。**
Excelは、数字に見えるkey（例: `12345e6789012345`）を指数表記に変えたり、文字コードやクォートを変えたりすることがあります。
VS Codeなどのテキストエディターか、すべての列を「テキスト」にしたLibreOfficeを使って、UTF-8のCSVで保存してください。

## 絵の翻訳

メニューのボタンや看板のように、文字が絵になっているところもあります。
訳した絵は `Translations/<locale>/textures/<ゲームのテクスチャ名>.png` に置いて、
`textures/credits.csv`（`file,author,note`）に行を足します。
絵は自分で描くか、ゲームの絵に手を加えて作ってください。
ゲームの絵をそのままコミットするのはだめです
（[コンテンツポリシー](https://github.com/TomXV/dragnwash-modframework/blob/main/docs/CONTENT_POLICY.ja.md)）。
手順は [docs/TRANSLATED_TEXTURES.ja.md](docs/TRANSLATED_TEXTURES.ja.md#翻訳する人の手順) にあります。

## ルール

- ゲーム本体のアセットやコードを、
  手を加えずにそのままコミットしない（[コンテンツポリシー](https://github.com/TomXV/dragnwash-modframework/blob/main/docs/CONTENT_POLICY.ja.md)）。
  自分で描いた絵や手を加えた絵は歓迎です（上の「絵の翻訳」を見てください）。
- `Translations/_discovered/` はコミットしない。
- 訳文は、それぞれ訳した人の貢献として扱います。ライセンスは [LICENSE](LICENSE) を見てください。

## 協力者のクレジット

PRの「Credit / クレジット表記」に、クレジットを載せてほしいかどうかと、出してほしい名前を書いてください。
載せてほしい場合は、PRをマージしたあとでメンテナーが別のコミットで追加するので、そのために何かしてもらう必要はありません。
クレジットが載るのは次の場所です。

- READMEの言語パックの表の、その言語の行
- その言語パックの `strings.csv` の先頭のコメント
- その変更が入ったリリースのリリースノート
- その言語パックの `credits.txt`（ゲーム内F1メニューのAboutタブに出ます。次のリリース以降）
- インストーラーのAbout（次のリリース以降）

パックの「状態」をどう書くか、たとえば一部を直しただけで「仮翻訳」の表記を変えるかどうかは、PRごとに決めます。
クレジットがいらない場合は何も足しません。
それでも、コミットはリポジトリの履歴に残ります。

## Issue について

- **ラベル:**
  新しい Issue には、種類・対象・深刻度のラベルが自動で付きます。
  不具合報告にバージョンや手順、ログが足りないときは、それを足してもらうようお願いするコメントが 1 回だけ付きます。
- **送るもの:**
  このために、タイトルと本文の文章と、貼られたログのエラー行を TypeSafe AI の分類モデルに送っています。
  本文のコードブロック・表・翻訳の行・画像は送らず、ログのパスに入っているユーザー名は伏せます。
  仕組みはフレームワークのリポジトリの [`tools/issue-triage.py`](https://github.com/TomXV/dragnwash-modframework/blob/main/tools/issue-triage.py) にあります。
  ゲームのテキストや翻訳ファイルは送りません。
- **Issue はすべて人が読みます。**
